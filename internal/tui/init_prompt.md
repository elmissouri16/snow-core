Create a repository guide named `AGENTS.md` in the current working directory.

Follow this workflow exactly:

1. Check whether `AGENTS.md` already exists in the current working directory before making any changes. If it exists, do not modify, replace, append to, rename, or delete it. Stop and report that initialization was skipped.
2. If it does not exist, inspect the repository with read and search tools before writing. Use only facts verified from the checkout; do not invent commands, tooling, workflows, conventions, or policies.
3. Write `AGENTS.md` in the current working directory as a concise, repository-specific contributor guide. Prefer the title `# Repository Guidelines` and clear Markdown headings. Include only relevant, evidence-backed sections such as project structure, build/test/development commands, coding style and naming, testing expectations, commit and pull-request conventions, and material security or configuration notes.
4. Use concrete paths and commands where useful. Keep the guide actionable and reasonably compact, targeting roughly 200–400 words unless the repository clearly needs more detail.
5. Finish by reporting whether `AGENTS.md` was created or initialization was skipped.

Do not merely propose content: inspect the repository and create the file when it is safe and permitted to do so. Continue to respect all configured permissions and filesystem boundaries.
